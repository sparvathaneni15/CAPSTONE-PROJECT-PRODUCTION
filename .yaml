paths:
  /predict:
    post:
      summary: "Make a prediction"
      parameters:
        - in: body
          name: input
          required: true
          schema:
            type: object
            properties:
              feature1:
                type: number
      responses:
        200:
          description: "Prediction result"